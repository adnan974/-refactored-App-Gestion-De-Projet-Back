import { ProjectAuthorization } from './project-authorization';

describe('ProjectAuthorization', () => {
  it('should be defined', () => {
    expect(new ProjectAuthorization()).toBeDefined();
  });
});
